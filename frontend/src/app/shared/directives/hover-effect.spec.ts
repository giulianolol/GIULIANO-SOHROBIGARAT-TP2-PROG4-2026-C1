import { HoverEffect } from './hover-effect.directive';

describe('HoverEffect', () => {
  it('should create an instance', () => {
    const directive = new HoverEffect();
    expect(directive).toBeTruthy();
  });
});
